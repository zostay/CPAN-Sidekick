<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qubling.sidekick"
    android:versionCode="12"
    android:versionName="@string/app_version" >

    <uses-sdk android:minSdkVersion="7" android:targetSdkVersion="13"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:theme="@style/Theme.Sidekick">
        <activity
            android:label="@string/activity_module_search_name"
            android:name=".ui.module.ModuleSearchActivity" android:configChanges="keyboardHidden|orientation">
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>
            <meta-data android:resource="@xml/module_searchable" android:name="android.app.searchable"/>
        </activity>
        <activity android:name=".ui.module.ModuleViewActivity" android:label="@string/activity_module_view">
        </activity>
        <activity android:label="@string/app_launcher_name" android:name=".SidekickLauncher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
